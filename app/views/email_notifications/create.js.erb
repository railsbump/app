{
  const $form = $(".email-notification-form")

  $form.find(".success, .error").hide()

  <% if @email_notification.valid? %>
    $form.find(".success").show()
  <% else %>
    $form.find(".error").show().find(".message").text("<%= j @email_notification.errors.full_messages.join(", ").html_safe %>')
  <% end %>
}
