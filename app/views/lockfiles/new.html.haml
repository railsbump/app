%h1
  Check a lockfile

= render "shared/flashes"

= form_with model: @lockfile do |form|

  .mb-3
    = form.text_area :content, class: "form-control autosize", placeholder: "Paste the content of your Gemfile.lock here."

  = form.submit "Check now", class: "btn btn-primary", data: { disable_with: t(:please_wait) }
