%section

  %h1
    Welcome to RailsBump! 👋

  %p
    RailsBump lets you check an individual Ruby gem or a whole Bundler lockfile (Gemfile.lock) for compatibility with all major and minor Rails versions since Rails 4.0.
    = link_to 'https://github.com/manuelmeurer/railsbump', target: '_blank' do
      Read more about how it works and the history of this project on GitHub
      &raquo;

%section
  = link_to 'Check a gem', %i(new gemmy), class: 'btn btn-primary'
  = link_to 'Check a lockfile', %i(new lockfile), class: 'btn btn-info'

%section
  %h2
    Latest checked gems
  = render 'compats/table', gemmies: @gemmies
